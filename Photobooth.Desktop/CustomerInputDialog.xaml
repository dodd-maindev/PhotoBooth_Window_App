<Window x:Class="Photobooth.Desktop.CustomerInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Joli Films - Thông tin khách hàng"
        Icon="pack://application:,,,/asset/Logo/joli-film-logo.png"
        Width="560"
        Height="420"
        WindowStartupLocation="CenterScreen"
        WindowStyle="SingleBorderWindow"
        ResizeMode="NoResize"
        Background="#170507"
        FontFamily="Bahnschrift">

    <Window.Resources>
        <LinearGradientBrush x:Key="BgGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#170507" Offset="0.00" />
            <GradientStop Color="#2A090C" Offset="0.52" />
            <GradientStop Color="#3A0D10" Offset="1.00" />
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="ButtonGradient" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="#B98A4A" Offset="0.00" />
            <GradientStop Color="#E3BF77" Offset="1.00" />
        </LinearGradientBrush>

        <Style x:Key="GoldButton" TargetType="Button">
            <Setter Property="Foreground" Value="#170507" />
            <Setter Property="Background" Value="{StaticResource ButtonGradient}" />
            <Setter Property="BorderBrush" Value="#8A6236" />
            <Setter Property="BorderThickness" Value="3" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="FontSize" Value="18" />
            <Setter Property="MinHeight" Value="52" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="32,16">
                            <Border.Effect>
                                <DropShadowEffect Color="#8A6236" BlurRadius="12" ShadowDepth="3" Opacity="0.6" Direction="270" />
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Opacity" Value="0.92" />
                                <Setter TargetName="Root" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#E8C786" BlurRadius="18" ShadowDepth="4" Opacity="0.7" Direction="270" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Opacity" Value="0.78" />
                                <Setter TargetName="Root" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#8A6236" BlurRadius="5" ShadowDepth="1" Opacity="0.5" Direction="270" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Root" Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#2A0F12" />
            <Setter Property="Foreground" Value="#FFF6E8" />
            <Setter Property="BorderBrush" Value="#7A4730" />
            <Setter Property="BorderThickness" Value="2" />
            <Setter Property="Padding" Value="18,18" />
            <Setter Property="FontSize" Value="22" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="CaretBrush" Value="#E8C786" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="6">
                            <Border.Effect>
                                <DropShadowEffect Color="#8A6236" BlurRadius="10" ShadowDepth="2" Opacity="0.4" Direction="270" />
                            </Border.Effect>
                            <ScrollViewer x:Name="PART_ContentHost" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#E8C786" />
                                <Setter TargetName="Border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#E8C786" BlurRadius="14" ShadowDepth="3" Opacity="0.5" Direction="270" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Background="{StaticResource BgGradient}">
        <Border Background="#2A0F12"
                BorderBrush="#7A4730"
                BorderThickness="1"
                Margin="24"
                Padding="28">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,6">
                    <TextBlock Text="JOLI FILM - SELFI BOOTH HANOI"
                               FontSize="18"
                               FontWeight="SemiBold"
                               Foreground="#E8C786"
                               HorizontalAlignment="Center" />
                    <TextBlock Text="34 ngõ 58 Đào Tấn, Ba Đình, Hà Nội"
                               FontSize="11"
                               Foreground="#D8BFA1"
                               HorizontalAlignment="Center"
                               Margin="0,4,0,0" />
                </StackPanel>

                <Separator Grid.Row="1"
                           Background="#5A241A"
                           Margin="0,12,0,18" />

                <TextBlock Grid.Row="2"
                           Text="Nhập tên khách hàng để bắt đầu phiên chụp:"
                           FontSize="14"
                           Foreground="#FFF6E8"
                           Margin="0,0,0,12" />

                <TextBox Grid.Row="3"
                         x:Name="CustomerNameTextBox"
                         Style="{StaticResource ModernTextBox}"
                         MaxLength="80" />

                <TextBlock Grid.Row="4"
                           Text="Mỗi khách hàng sẽ có folder riêng để lưu ảnh."
                           FontSize="11"
                           Foreground="#D8BFA1"
                           Margin="0,10,0,0"
                           TextWrapping="Wrap" />

                <StackPanel Grid.Row="5"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            Margin="0,20,0,0">
                    <Button x:Name="StartButton"
                            Style="{StaticResource GoldButton}"
                            Content="BẮT ĐẦU CHỤP"
                            Click="StartButton_Click"
                            IsDefault="True"
                            MinWidth="220"
                            MinHeight="52" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
